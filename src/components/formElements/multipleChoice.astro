---
import "../../styles/global.css"

const {label, id} = Astro.props
---
<fieldset class="fieldset">
    <legend class="fieldset-legend font-normal">{label}</legend>
    <select class="select" id={id}>
      <slot />
    </select>
  </fieldset>